# Rust
/target/

# Swift
ui/swift/.build/
ui/swift/.swiftpm/

# Bundles / build output
/build/*.app/
/build/*.framework/
/build/*.dylib/

# Local runtime data (graph snapshots per project) — anywhere in the tree
**/.spire/

# macOS
.DS_Store

# Editor config (local)
.vscode/
